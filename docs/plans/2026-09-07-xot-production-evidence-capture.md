# Production release evidence capture — 2026-09-07

Target: `jzirqfzzvlbxwfzndaer`, public schema. Owner approved protected local
schema, migration-history and generated-type capture, excluding row-data export,
cloud upload and Production writes. No release or gate waiver was approved.

## Captured evidence

Raw files are outside Git in the owner-only directory
`/private/tmp/xot-production-release-evidence.uVYWRp/` (0700); data files are
0600. They must not be uploaded or committed. This temporary directory is not
a durable backup or restore proof. Capture scripts preserve command exit status,
timestamp, output size and SHA-256 without printing raw database definitions.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| remote-migration-snapshot.json | 681263 | b9047ddbf06bcc1cf9fed2d037ef25c88188f0aa42c1e45e97e235972442509d |
| production-schema-catalog.json | 562413 | 124c11090f8045f03d8b5391c209bca499c66042b69f5ae15910e1bd96eb4fdc |
| production-types.ts | 111114 | 1527e399c86eeef11ac3342f1196b9083cb8ef6e0577793d211f39d1eb7a4c5d |

Types were generated by installed Supabase CLI 2.111.0 with explicit project ID
and public schema at 01:37:39 UTC. History/schema were captured at approximately
01:40:28–29 UTC with `BEGIN READ ONLY` through the bundled Go CLI's linked query
command. Its target was isolated using the exact Production ref and the
workspace's verified existing credential-free pooler identity. The actual
workspace link/configuration was not changed. Queries read migration metadata
and system catalogs, not application table records.

The first attempts failed because the local network cannot reach direct IPv6.
No CLI update, network permission change, remote relink, database migration,
application-row mutation, restore, deployment or image pull was performed.

## Important unexpected preparation side effect

Successful linked query preparation printed `Initialising login role...`.
Inspection of the version-matched CLI connection resolver confirmed that linked
connection preparation can mint a temporary login role before executing SQL.
This is outside the approved no-Production-write capture scope, despite the
queries themselves using read-only transactions. The operator disclosed the
side effect and stopped further CLI use. Do not describe the whole command as
zero-write merely because its SQL was read-only.

Read-only connector inspection at 01:43:19 UTC found `cli_login_postgres` with
credential expiry `2026-09-07 01:45:29.311961+00`, no superuser/create-role/create-DB
privileges, and zero active sessions. There is no before-state evidence proving
whether the role itself was newly created or an existing role was refreshed.
No manual role cleanup/revocation was attempted. Credential expiry does not
mean the role object has been dropped or every authentication method revoked.
At `2026-09-07 01:46:00.918726+00`, a fresh read-only connector query confirmed
the recorded credential expiry had passed and active sessions were zero. No
credential-refreshing CLI command ran after the side effect was detected.

Version-matched source:
[CLI resolver](https://github.com/supabase/cli/blob/v2.111.0/apps/cli/src/legacy/shared/legacy-db-config.service.ts),
[dump preparation](https://github.com/supabase/cli/blob/v2.111.0/apps/cli/src/legacy/commands/db/dump/dump.handler.ts).
Context7 `/supabase/cli` was used for command behavior, then the installed
version's source/runtime was checked; no latest-version upgrade was inferred.

## Verified comparison

- 117 remote migration entries; all 105 historical observed bodies remain
  hash-identical, with no missing historical versions. Twelve later entries are
  absent from that historical anchor. They are not automatically approved.
- Historical version `20260516050042` still has no available statement body.
- Preview repair `20260907001640` is not in Production migration history.
- Schema catalog captures 661 columns, 130 constraints, 210 functions, 187 indexes,
  44 policies, 23 triggers, 4 views, 1 sequence, 2 enum values and 6 default ACL
  records. It also records relation/schema owners, ACLs, RLS flags and definitions.
- Generated types differ byte-for-byte from the checked-in file. An AST-based
  public-schema inventory has matching 45 tables, 4 views, 85 functions and one
  enum, with identical whitespace-normalized table/view/enum type members.
  Six function type members differ: `activate_runtime_v2`,
  `claim_video_render_after`, `current_user_role`, `update_runtime_control`,
  `update_runtime_controls`, `update_runtime_controls_v2`. This is a targeted
  textual type comparison, not full semantic equivalence or migration approval.

## Release gate remains closed

The captured schema catalog is **not canonical pg_dump SQL** and must not be
passed as `--production-schema` or substituted for replay/dump equivalence.
The current capture envelope records truthful CLI provenance, which differs
from the old gate's pinned MCP capture-tool/query contract; hashes and metadata
were not falsified to make that gate pass. The historical manifest also pins
105 remote rows and an older exact export hash, not the current 117-row capture.
Direct `validateRemoteSnapshot` execution also detected credential-shaped
JWT/Bearer content in historical statements. Only labels/locations were
reported, never values. Raw originals remain protected; silently redacting them
would invalidate their byte-evidence and is not a valid way to pass the gate.

Still required: canonical public-schema dump through an approved connection
path, reviewed candidate/provenance reconciliation, isolated no-egress replay,
restore proof, privilege review and fresh hosted candidate evidence. Neither
local capture nor a locally written receipt impersonates owner approval.
Raw protected evidence is time-sensitive under the gate's six-hour window.

## Canonical schema dump completed after explicit approval — 05:17 UTC

The owner subsequently approved temporary-login initialization solely for the
read-only public-schema dump. The existing CLI generated its dump script at
05:16:29 UTC. That script was inspected for schema-only/public selection and
executed unchanged through stdin in an existing local image:
`public.ecr.aws/supabase/postgres:17.6.1.143`. No image download was needed.
The container used one CPU, 512 MiB memory, a read-only filesystem, 32 MiB
temporary space, no host/workspace mount, TLS-required database access and
`PGOPTIONS=-c default_transaction_read_only=on -c statement_timeout=45000`.

- Dump exit 0 at `2026-09-07T05:17:34.719Z`.
- Artifact: `production-schema.sql`, 300121 bytes, mode 0600, SHA-256
  `8d42bd1648794c0d113ac15942741d60fca13ad69c2929a3ff2248f9aab8738f`.
- Contains 45 tables, 44 policies, 96 emitted function definitions and 132
  explicit indexes. Catalog counts also include extension-owned objects and
  implicit constraint indexes; the two formats are not count-equivalent.
- No `COPY ... FROM stdin` statement. Schema-only selection is established by
  the inspected CLI command, not by scanning arbitrary function bodies for DML.
- Exact task container `xot-schema-dump-20260907-0516` was auto-removed;
  independent `docker ps -a` queries found no remaining matching container.
- No application-row writes, migrations, restore, deployment or private upload.

The type differences were then narrowed with a strict local TypeScript compile:
all table/view types and the Args/Returns of five differing functions are
mutually assignable. Differences include property ordering, equivalent Row
aliases and generated SetofOptions metadata. `current_user_role` differs only
by the checked-in explicit null allowance. No generated types were overwritten
and these checks do not waive the byte-bound release evidence requirement.

### Gate invocation with actual captured inputs

The first full invocation rejected evidence outside the repository root. Four
inputs were consequently copied, byte/hash-verified, into the existing ignored
`supabase/.temp/release-evidence-20260907/` directory (0700, files 0600).
`git check-ignore` confirmed exclusion. No ignore rule or validator was changed;
these raw sensitive files must never be force-added, uploaded or shared.

The second invocation consumed those paths and exited 1 with 100 diagnostic
items spanning the existing candidate inventory/immutable-source mismatch,
credential-bearing historical statements, truthful capture provenance differing
from the old pinned MCP/linked-command contract, missing protected replay,
missing reviewed candidate, and type/hash-manifest mismatch. The inputs are
captured, **not accepted**. The current type receipt records its actual command
and `reviewed_git_sha=null`; it does not fabricate reviewer approval.

The canonical-dump prerequisite is now complete. Remaining work is reviewed
baseline/provenance reconciliation plus isolated replay, restore, privilege and
fresh hosted-candidate evidence. Production rollout remains closed.

Final read-only verification at `2026-09-07T05:22:06.985167Z`: the approved
temporary login credential expiry `05:21:29.253573Z` had passed, with zero
active sessions. The role object was not manually deleted or altered.

## Current-history reconciliation and isolated repair test — 07:45 UTC

The owner's instruction to explain simply and continue was followed with local
analysis and a focused disposable regression, not a Production migration.

- Captured history contains 117 entries; local inventory contains 137 files.
  All 105 historical remote entries retain their captured bodies. Of the 12
  newer remote entries, ten match existing local migration bodies exactly,
  allowing only an optional terminal LF. The two unmatched entries are
  `20260825103600_v1_delivery_continuity_cutover` and
  `20260901135245_xot_v2_production_convergence_fbbd44a`; bundle similarity is
  not equivalence or approval. No bulk migration push is authorized.
- A fresh read-only aggregate observation around 07:38 UTC matched the captured
  ordered migration-history MD5 `f1fd78002feb2dc954bcfe576b45968e` (version plus
  per-body digest). This detects history drift only; it does not renew the
  capture's six-hour freshness window or establish full schema equivalence.
- Focused test completed at `2026-09-07T07:45:46.947Z`, using the existing cached
  image digest `80d7b27c3e8d77cfa7226eee9508671796da214781ff15a35b3670d7ad5ee453`,
  one CPU, 1 GiB memory, network `none`, zero host mounts and zero published ports.
  No image download, hosted writes, application-row export or provider call.
- Loaded the captured public-schema SQL with explicit local prerequisites:
  existing E7 auth/storage placeholders, bundled vector extension, empty cron
  table, and the checked-in `private.has_role` definition. The public dump omits
  that cross-schema dependency. This is **not** a complete Production restore,
  authentication/RLS acceptance, backup recovery proof, or pinned E7/E10 replay.
- The captured feedback function reproduced the ambiguous-column error before
  the patch. Applying only `20260907001640_video_render_feedback_qualified_columns.sql`
  then passed missing-render, stale-revision, insert/readback, result fields,
  metadata, feedback-count, and anon/authenticated direct-RPC denial checks.
  The existing guarded Preview SQL regression ran unchanged against synthetic
  local fixtures. Rollback left zero feedback rows.
- Earlier attempts exposed startup restart timing, missing vector, and missing
  private-schema prerequisites; these were corrected in the disposable harness.
  No repository gate or test assertion was weakened. All four task containers
  and their disposable databases were removed after their attempts.

Protected scripts and receipts remain under
`/private/tmp/xot-production-release-evidence.uVYWRp/`, including
`candidate-reconciliation.json`, `test-repair.mjs`, and `repair-test-receipt.json`.
These are local evidence, not published or owner-approved release receipts.

### Actual next boundary

The focused repair is compatible with the captured public-schema environment.
The required release gate is still closed. Its historical capture contract,
immutable candidate evidence and E7/E10 replay fixtures do not describe this
117-remote/137-local candidate. The exact pinned replay image
`sha256:99b1729aeb0bac314445024fc149fbd39306170b61dd50800ccf180327ab3459`
is not cached. The alternative cached image used above cannot substitute for it.
Changing standing release-policy/tooling contracts and downloading the missing
runtime need explicit scope approval under the owner's instructions; no such
changes or download were inferred. Reviewed reconciliation, full required replay,
restore/privilege evidence and fresh exact-candidate hosted CI remain outstanding.
No merge, push, deployment or Production migration was performed in this pass.

## Superseding authorized baseline/tooling result — 10:36 UTC

The owner subsequently approved the current-baseline/tooling update and exact
test-image download. The current 137-migration SQL/type replay now passes on the
original pinned runtime with a direct no-egress probe and verified cleanup.
Historical receipts remain unchanged. See the
[current release-gates handoff](2026-09-07-xot-current-release-gates.md) for the
new baseline, source-bound replay, redacted comparison and actual remaining
owner/recovery/CI gates. This supersedes the missing-runtime/tooling block above,
not the Production release hold or expired-capture limitation.
